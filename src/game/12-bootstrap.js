/* -------------------------------- bootstrap -------------------------------- */
addEnvironment();addLabObjects();player.yaw=Math.PI/2;player.forward=V.v(1,0,0);updateChecks();initYandex();requestAnimationFrame(loop);

