'use strict';
(()=>{const G=window.GAME,S=G.state,{V,sat2}=G,oldLoad=G.loadLevel;
function bodyBox(def,x,y,z,yaw=0){return{pos:{x,y,z},w:def.w,d:def.d,h:def.h,yaw}}
function free(def,x,y,z,yaw=0,ignoreSupport=null){const a=bodyBox(def,x,y,z,yaw),a0=y-def.h/2,a1=y+def.h/2;for(const s of S.statics){const s0=s.y-s.sy/2,s1=s.y+s.sy/2;if(Math.min(a1,s1)-Math.max(a0,s0)<=.015)continue;const bb={pos:{x:s.x,z:s.z},w:s.sx,d:s.sz,yaw:s.yaw||0};if(sat2(a,bb))return false}for(const b of S.bodies){if(b===ignoreSupport)continue;const yov=Math.min(a1,b.pos.y+b.h/2)-Math.max(a0,b.pos.y-b.h/2);if(yov<=.012)continue;if(sat2(a,b))return false}return true}
function spawn(id,x,z,opt={}){const d=G.objectById[id];if(!d)return null;const yaw=opt.yaw||0,y=opt.y??d.h/2;if(!opt.force&&!free(d,x,y,z,yaw,opt.support))return null;const b=G.makeBody({...d,x,y,z,yaw,tag:'env:'+id,objective:false});b.catalogId=id;b.fragile=d.fragile;b.breakImpulse=d.breakImpulse;b.damageState='INTACT';b.spawn={...b.pos};b.spawnYaw=yaw;S.bodies.push(b);return b}
function topSpawn(id,support,dx=0,dz=0,yaw=0){const d=G.objectById[id];if(!d||!support)return null;const q=G.rot2(dx,dz,support.yaw||0),y=support.pos.y+support.h/2+d.h/2+.008;return spawn(id,support.pos.x+q.x,support.pos.z+q.z,{y,yaw,force:true,support})}
function officeSet(){const desks=[];for(const p of [[-5.0,-3.0,0],[-.5,3.0,Math.PI],[4.0,-3.0,0]]){const d=spawn('coffee_table',p[0],p[1],{yaw:p[2]});if(d)desks.push(d)}const shelves=[spawn('bookcase',-2.6,-3.5,{yaw:0}),spawn('shelf_unit',5.8,3.3,{yaw:Math.PI})].filter(Boolean);for(const [i,d] of desks.entries()){topSpawn('office_monitor',d,-.18,0,i?Math.PI:0);topSpawn('keyboard',d,.12,-.08,i?Math.PI:0);topSpawn('mouse',d,.36,-.05,0);if(i===0)topSpawn('mug',d,-.36,.1,0);if(i===1){topSpawn('paper_stack',d,-.32,.12,0);topSpawn('stapler',d,.36,.12,0)}if(i===2){topSpawn('laptop',d,-.12,.03,0);topSpawn('desk_phone',d,.34,.10,0)}}for(const p of [[-4.8,-1.8,.2],[.2,1.7,-.2],[3.8,-1.8,.4]])spawn('wood_chair',...p);for(const p of [[-6.7,3.1,0],[7,-2.8,0]])spawn('plantpot',...p);for(const p of [[-6.5,-2.5,0],[5.9,2.4,0]])spawn('trashcan',...p);for(const sh of shelves){topSpawn('book',sh,-.25,0,0);topSpawn('binder',sh,.18,0,0);topSpawn('folder',sh,.38,0,0)}}
function apartmentSet(){const t=spawn('coffee_table',-1.5,2.5,{yaw:.2});if(t){topSpawn('mug',t,-.2,0,0);topSpawn('book',t,.25,.05,.2)}spawn('tv',4.6,-3.2,{yaw:0});spawn('nightstand',5.5,2.8,{yaw:0});spawn('desk_lamp',5.5,2.8,{y:.65+.58/2,force:true});spawn('basket',-5.5,3.0);spawn('plantpot',-6.5,-3.0);spawn('wood_chair',1.2,-2.7,{yaw:-.5});spawn('vacuum',6.5,0,{yaw:1.2})}
function warehouseSet(){for(const p of [[-6,-3,0],[-2.5,3,.2],[1.5,-3,-.15],[5,3,.1]])spawn('pallet_box',...p);for(const p of [[-5.8,-3,0],[-2.3,3,0],[1.7,-3,0],[5.2,3,0]])spawn('large_crate',p[0],p[1],{yaw:p[2]});spawn('toolbox',-4.2,2.0);spawn('pipe',3.5,-2.2,{yaw:.8});spawn('plank',5.2,-2.4,{yaw:-.5});spawn('sack',-.5,2.3);spawn('cone',6.8,2.8)}
function kitchenSet(){const t=spawn('coffee_table',-3.8,2.6);if(t){topSpawn('kettle',t,-.25,0,0);topSpawn('plate',t,.2,0,0);topSpawn('mug',t,.38,.08,0)}spawn('microwave',3.0,-3.0);spawn('bucket',5.6,3.0);spawn('mop',6.4,2.7,{yaw:1.2});spawn('basket',-6.7,3.0)}
function storeSet(){for(const p of [[-5,-3,0],[-1,3,Math.PI],[3,-3,0]])spawn('shelf_unit',...p);for(const p of [[-6.3,2.5,0],[5.8,2.7,0]])spawn('large_crate',...p);spawn('bottle',1.4,2.2);spawn('jar',1.8,2.2);spawn('basket',4.8,-2.5);spawn('cone',7,2.8)}
function hotelSet(){const t=spawn('nightstand',-4.8,2.8);if(t){topSpawn('desk_lamp',t,0,0,0);topSpawn('glass',t,.18,.08,0)}spawn('wood_chair',3.5,-2.8,{yaw:.4});spawn('basket',5.5,2.6);spawn('plantpot',-6.5,-2.8);spawn('magazine',2.2,2.7)}
function museumSet(){spawn('bench',-4.8,3.0);spawn('bench',3.5,-3.0,{yaw:Math.PI});spawn('bookcase',6.0,3.0);spawn('plantpot',-7,-3.0);spawn('glass',1.5,2.8);spawn('bowl',2.0,2.8)}
function addInteractiveEnvironment(){if(S.levelIsLab)return;const t=S.level?.theme;if(t==='office')officeSet();else if(t==='apartment')apartmentSet();else if(t==='warehouse'||t==='garage')warehouseSet();else if(t==='kitchen')kitchenSet();else if(t==='store')storeSet();else if(t==='hotel')hotelSet();else if(t==='museum')museumSet();else{spawn('small_crate',-5,3);spawn('wood_chair',3,-3);spawn('plantpot',6,3)}}
G.spawnCatalogItem=spawn;G.loadLevel=(index,opt={})=>{oldLoad(index,opt);addInteractiveEnvironment();return S.level};
G.loadObjectGallery=()=>{oldLoad(0,{lab:true});S.levelIsLab=true;S.level={...S.level,name:'OBJECT TEST GALLERY',theme:'lab',props:[]};S.bodies=[];S.required=[];const cols=11,spacing=2.15;for(let i=0;i<G.objectCatalog.length;i++){const d=G.objectCatalog[i],x=(i%cols-cols/2)*spacing,z=(Math.floor(i/cols)-4.2)*spacing,b=spawn(d.id,x,z,{force:true});if(b){b.pos.y=d.h/2;b.spawn={...b.pos}}}S.player.pos=V.v(-12,.9,0);S.goal={x:12,z:0,w:2,d:2};G.UI.level.textContent=`OBJECT TEST GALLERY · ${G.objectCatalog.length}`;G.UI.cargo.textContent='DEV · ВСЕ ТИПЫ ОБЪЕКТОВ';G.rendererResetCamera?.()};
})();