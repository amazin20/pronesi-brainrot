'use strict';
(()=>{const G=window.GAME;let ctx=null;function audio(){try{ctx=ctx||new(window.AudioContext||window.webkitAudioContext)();if(ctx.state==='suspended')ctx.resume();return ctx}catch{return null}}
const profile={wood:[180,'triangle',.11],metal:[105,'square',.16],fabric:[90,'sine',.055],rubber:[150,'sine',.08],stone:[68,'triangle',.13],plastic:[210,'square',.07],cardboard:[120,'triangle',.055],ceramic:[720,'sine',.10],glass:[1180,'sine',.085],concrete:[62,'triangle',.13]};
G.impactSound=(mat='wood',force=3)=>{const c=audio();if(!c)return;try{const [base,type,dur]=profile[mat]||profile.wood,t=c.currentTime,o=c.createOscillator(),gain=c.createGain(),fragileTone=mat==='glass'||mat==='ceramic',decay=fragileTone?.58:.46;o.type=type;o.frequency.setValueAtTime(base+Math.min(base*.65,force*18),t);o.frequency.exponentialRampToValueAtTime(Math.max(38,base*decay),t+dur);gain.gain.setValueAtTime(Math.min(.12,.012+force*.006),t);gain.gain.exponentialRampToValueAtTime(.0001,t+dur);o.connect(gain);gain.connect(c.destination);o.start(t);o.stop(t+dur+.015);if(['stone','concrete','cardboard','wood'].includes(mat)&&force>3){const n=160,buf=c.createBuffer(1,n,c.sampleRate),d=buf.getChannelData(0);for(let i=0;i<n;i++)d[i]=(Math.random()*2-1)*(1-i/n);const src=c.createBufferSource(),ng=c.createGain();src.buffer=buf;ng.gain.setValueAtTime(Math.min(.04,force*.003),t);src.connect(ng);ng.connect(c.destination);src.start(t)}}catch{}};
G.grabSound=()=>{const c=audio();if(!c)return;try{const t=c.currentTime,o=c.createOscillator(),g=c.createGain();o.type='sine';o.frequency.setValueAtTime(260,t);o.frequency.exponentialRampToValueAtTime(180,t+.045);g.gain.setValueAtTime(.025,t);g.gain.exponentialRampToValueAtTime(.0001,t+.055);o.connect(g);g.connect(c.destination);o.start(t);o.stop(t+.06)}catch{}};
const oldStart=G.startGrip;G.startGrip=hit=>{const ok=oldStart?.(hit);if(ok)G.grabSound();return ok};
})();