import fs from 'node:fs';import path from 'node:path';const sha=(process.env.GITHUB_SHA||process.env.BUILD_SHA||'dev').slice(0,12),dist='dist';fs.rmSync(dist,{recursive:true,force:true});fs.mkdirSync(dist,{recursive:true});
for(const f of ['style.css','qa-runner.js'])fs.copyFileSync(f,path.join(dist,f));fs.cpSync('src',path.join(dist,'src'),{recursive:true});if(fs.existsSync('demo'))fs.cpSync('demo',path.join(dist,'demo'),{recursive:true});if(fs.existsSync('playtest-v2'))fs.cpSync('playtest-v2',path.join(dist,'playtest-v2'),{recursive:true});let html=fs.readFileSync('index.html','utf8');html=html.replace('<title>','<script>window.__BUILD_SHA__='+JSON.stringify(sha)+';<\/script>\n<title>');html=html.replace(/href="style\.css"/g,`href="style.css?v=${sha}"`).replace(/src="(src\/overhaul\/[^"]+\.js|qa-runner\.js)"/g,(_,p)=>`src="${p}?v=${sha}"`);fs.writeFileSync(path.join(dist,'index.html'),html);fs.writeFileSync(path.join(dist,'build.json'),JSON.stringify({sha,builtAt:new Date().toISOString()},null,2));fs.writeFileSync(path.join(dist,'.nojekyll'),'');console.log(`Prepared ${dist} for build ${sha}`);
