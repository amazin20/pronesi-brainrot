name: Second Pit Carrier WebGL Budget v105

on:
  push:
    branches: [new-player-glb-overhaul]
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Verify second pit carrier WebGL budget
        run: node scripts/second-pit-carrier-webgl-budget-v105-check.mjs
